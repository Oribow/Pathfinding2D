using NavGraph.Build;
using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using UnityEngine;

class ContourOverlapTree
{/*
    List<Node> bottomNodes;

    public ContourOverlapTree()
    {
        
    }

    public void AddColliderToTree(ContourCache.ColliderInfo collider, Contour contour)
    {
        bool wasContourUsed = false;
        ContourNode result;
        foreach (var bottomNode in bottomNodes)
        {
            bottomNode.contour.AddSolidContour(contour, ref wasContourUsed);
            if(bottomNode.contour.TryCombineAsNew())
            {

            }
        }
    }

    public class Node
    {
        //Graph stuff
        Node parent;

        //Data
        public ContourNode contour;
    }*/
}
